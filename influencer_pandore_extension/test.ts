// chrome.runtime.sendMessage({ action: "getUserData" }, (response) => {
//     if (chrome.runtime.lastError) {
//       console.error("Erreur de communication avec background:", chrome.runtime.lastError.message);
//       // Fall-back ou gestion de l'absence de réponse
//     } else {
//       console.log("Données utilisateur reçues:", response.userData);
//     }
//   });
  